// Modify the insert function in the hash table example to handle updating a value if the key already exists, instead of just adding a duplicate.

// Add a function void printHashTable() that iterates through the hash table and prints all key-value pairs.